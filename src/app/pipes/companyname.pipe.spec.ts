import { CompanynamePipe } from './companyname.pipe';

describe('CompanynamePipe', () => {
  it('create an instance', () => {
    const pipe = new CompanynamePipe();
    expect(pipe).toBeTruthy();
  });
});
