import { QualificationPipe } from './qualification.pipe';

describe('QualificationPipe', () => {
  it('create an instance', () => {
    const pipe = new QualificationPipe();
    expect(pipe).toBeTruthy();
  });
});
